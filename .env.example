# Copy to .env / set in your shell. Never commit real secrets.

# --- Auth (optional) ---
# node scripts/hash-password.mjs "your-password"
# AUTH_SECRET=change-me-to-a-long-random-string
# AUTH_USERS=ashok:$2b$10$replaceWithBcryptHash

# --- Server ---
# PORT=4173
# DATABASE_PATH=C:\Trade Analysis\ashok-trade-analysis\data\asx.sqlite

# API_RATE_LIMIT=180   # max /api/series requests per IP per minute

# --- Index membership ---
# Prefer: npm run refresh:asx200
# Optional override:
# INDEX_ASX200_CSV=data/asx200-official.csv
